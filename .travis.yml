sudo: required

language:
  - python
  - cpp

python:
  - "2.7"
  - "3.6"
  - "3.7"

# public repository on Travis CI
install:
  - pip install codecov

after_success:
  - codecov
  - coverage xml
  - python-codacy-coverage -r coverage.xml
  - coverage report
