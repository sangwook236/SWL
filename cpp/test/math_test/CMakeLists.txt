set(TARGET swl_math_test)
set(SRCS main.cpp)
if(CMAKE_BUILD_TYPE MATCHES DEBUG)
    set(LIBS swl_math)
else(CMAKE_BUILD_TYPE MATCHES DEBUG)
    set(LIBS swl_math)
endif(CMAKE_BUILD_TYPE MATCHES DEBUG)

add_executable(${TARGET} ${SRCS})
target_link_libraries(${TARGET} ${LIBS})
