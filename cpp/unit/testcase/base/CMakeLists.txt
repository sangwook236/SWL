set(BASE_SRCS LoggerTest.cpp)
if(CMAKE_BUILD_TYPE MATCHES DEBUG)
    set(BASE_LIBS swl_base)
else(CMAKE_BUILD_TYPE MATCHES DEBUG)
    set(BASE_LIBS swl_base)
endif(CMAKE_BUILD_TYPE MATCHES DEBUG)
