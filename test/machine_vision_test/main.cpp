#include "stdafx.h"
#include <iostream>

int main()
{
	void hough_transform();

	hough_transform();

	std::wcout << L"press any key to exit !!!" << std::endl;
	std::wcout.flush();
	std::wcin.get();

    return 0;
}

